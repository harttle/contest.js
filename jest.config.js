/**
 * @type {import('ts-jest/dist/types').InitialOptionsTsJest}
 */
module.exports = {
  // uncomment this if need type-checking when testing
  // preset: 'ts-jest',
  testEnvironment: 'node',
  modulePathIgnorePatterns: ['lib']
}
